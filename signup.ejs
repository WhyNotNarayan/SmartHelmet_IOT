<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup | Smart Helmet</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <style>
    :root {
      --glass-bg: rgba(255, 255, 255, 0.05);
      --glass-border: rgba(255, 255, 255, 0.1);
      --accent-neon: #00ffaa; /* Fresh green-cyan accent for Signup */
    }

    body { 
      background: radial-gradient(circle at center, #0f172a 0%, #020617 100%); 
      min-height: 100vh; 
      display: flex; 
      align-items: center; 
      justify-content: center; 
      color: white;
      font-family: 'Segoe UI', sans-serif;
      padding: 40px 0;
    }

    .signup-card { 
      width: 480px; 
      padding: 40px; 
      border-radius: 28px; 
      background: var(--glass-bg);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid var(--glass-border);
      box-shadow: 0 25px 50px rgba(0,0,0,0.6);
    }

    .brand-icon {
      font-size: 2rem;
      color: var(--accent-neon);
      background: rgba(0, 255, 170, 0.1);
      width: 60px;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 15px;
      margin: 0 auto 20px;
      box-shadow: 0 0 20px rgba(0, 255, 170, 0.2);
    }

    h2 { font-weight: 700; margin-bottom: 5px; }
    p.subtitle { color: #8e9aaf; font-size: 0.9rem; margin-bottom: 30px; }

    .form-label { 
      font-size: 0.75rem; 
      font-weight: 700; 
      color: #94a3b8; 
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-left: 2px;
    }

    .form-control {
      background: rgba(15, 23, 42, 0.5) !important;
      border: 1px solid var(--glass-border) !important;
      color: white !important;
      padding: 10px 15px;
      border-radius: 10px;
      transition: all 0.3s ease;
      font-size: 0.95rem;
    }

    .form-control:focus {
      border-color: var(--accent-neon) !important;
      box-shadow: 0 0 12px rgba(0, 255, 170, 0.15) !important;
    }

    .btn-signup {
      background: linear-gradient(45deg, #00ffaa, #00b4d8);
      border: none;
      color: #020617;
      padding: 12px;
      border-radius: 12px;
      font-weight: 700;
      text-transform: uppercase;
      margin-top: 15px;
      transition: 0.3s;
    }

    .btn-signup:hover {
      box-shadow: 0 0 20px rgba(0, 255, 170, 0.4);
      transform: translateY(-2px);
      color: #020617;
    }

    .login-link {
      color: #94a3b8;
      text-decoration: none;
      font-size: 0.9rem;
      transition: 0.3s;
    }
    .login-link span { color: var(--accent-neon); font-weight: 600; }
    .login-link:hover { color: white; }

    /* Custom scrollbar for the card if content overflows */
    ::-webkit-scrollbar { width: 5px; }
    ::-webkit-scrollbar-thumb { background: var(--glass-border); border-radius: 10px; }
  </style>
</head>
<body>

  <div class="signup-card">
    <div class="text-center">
      <div class="brand-icon">
        <i class="bi bi-person-plus-fill"></i>
      </div>
      <h2>Join the Network</h2>
      <p class="subtitle">Secure your ride with Guardian AI Smart Helmet</p>
    </div>

    <form action="/auth/signup" method="POST">
      <div class="row">
        <div class="col-md-12 mb-3">
          <label class="form-label">Username</label>
          <input type="text" name="username" class="form-control" placeholder="JohnDoe" required>
        </div>
        
        <div class="col-md-12 mb-3">
          <label class="form-label">Email Address</label>
          <input type="email" name="email" class="form-control" placeholder="john@example.com" required>
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label">Phone</label>
          <input type="text" name="phone" class="form-control" placeholder="+123456789">
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label">Vehicle No.</label>
          <input type="text" name="vehicle" class="form-control" placeholder="ABC-1234">
        </div>

        <div class="col-md-12 mb-4">
          <label class="form-label">Password</label>
          <input type="password" name="password" class="form-control" placeholder="••••••••" required>
        </div>
      </div>

      <button type="submit" class="btn btn-signup w-100">
        Create Account <i class="bi bi-check-circle-fill ms-2"></i>
      </button>
    </form>

    <div class="text-center mt-4">
      <a href="/auth/login" class="login-link">
        Already registered? <span>Sign In</span>
      </a>
    </div>
  </div>

</body>
</html>