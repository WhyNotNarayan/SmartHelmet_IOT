<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Logging Out | Smart Helmet</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <style>
    body {
      background: #0f172a;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Inter', sans-serif;
      color: white;
      margin: 0;
    }

    .logout-container {
      text-align: center;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(15px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      padding: 3rem;
      border-radius: 30px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.5);
    }

    .spinner-border {
      width: 4rem;
      height: 4rem;
      color: #00f2fe;
      border-width: 0.25em;
    }

    .status-text {
      font-size: 1.5rem;
      font-weight: 700;
      margin-top: 20px;
      background: linear-gradient(to right, #4facfe, #00f2fe);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .security-note {
      color: #94a3b8;
      font-size: 0.9rem;
      margin-top: 15px;
    }
  </style>
</head>
<body>

  <div class="logout-container">
    <div class="spinner-border mb-3" role="status">
      <span class="visually-hidden">Loading...</span>
    </div>
    <div class="status-text">Disconnecting Session...</div>
    <p class="security-note">Closing secure link to Helmet.IO Diagnostics</p>
  </div>

  <script>
    // Simulate a brief delay for a "Pro" feel, then redirect to the actual logout route
    setTimeout(() => {
        window.location.href = "/auth/login"; 
    }, 2000);
  </script>

</body>
</html>